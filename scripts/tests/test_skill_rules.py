#!/usr/bin/env python3
"""提示词压缩回归：逐条断言「旧版里的可执行规则」在新版里仍在（随仓库走，路径相对脚本自身）。

每条 = (技能, 说明, 必须出现的片段)。片段取自旧版原文里**承载规则**的词
（阈值、字段名、文件名、命令、时机、禁止项），不是文风词。
"""
import json
import os
import re
import sys
from pathlib import Path

SK = str(Path(__file__).resolve().parents[2] / '.dsh' / 'skills')

RULES = {
'learning-system': [
 ('开场先定位工作区', 'studymate-config.yaml'),
 ('记 workspace 为 LEARN_WORKSPACE', 'LEARN_WORKSPACE'),
 ('缺配置提示 npx 安装', 'npx -y @yunmiao/studymate@latest install'),
 ('按恢复视图读状态', '恢复视图'),
 ('首次使用问三件事', '想学什么'),
 ('新科目落地顺序：科目文件夹', 'assessments/'),
 ('MISSION 两节来自盘问第一问', 'Success looks like'),
 ('新科目先派 resource-scout 收集资料', '`resource-scout` 收集资料'),
 ('新科目资源清单：角色落 deliver/、总控 cp 落位', '它把清单草稿落到 `<subject_path>/.stage/resource-scout-<slug>/deliver/RESOURCES.md`'),
 ('prompt 只写三样（规格是权威、只给值）', '**prompt 只写三样**（**规格是权威，你只给值**）'),
 ('prompt 不写：要加载的协议／交付格式／写盘口径／做法判据', '**不写进 prompt**（角色规格里都有）：要它加载哪些协议'),
 ('progress 初始值', 'misconceptions: []'),
 ('project 只写 current', 'project` 只写 `current'),
 ('过 progress schema', 'progress.schema.json'),
 ('报告要给下一步', '不许只报告就停'),
 ('给可点链接与绝对路径', 'present'),
 ('盘问画设计树/分轮', 'frontier'),
 ('推荐项第一', '(Recommended)'),
 ('必问项之一：目的追问到具体', '追问到具体'),
 ('必问项之二：目标层级别与目的合并（档位）', '能独立做项目 / 精通'),
 ('必问项之四：项目给 3 个以上候选', '3 个以上候选'),
 ('必问项之五：实验载体先随派工交给 practice-evaluator', '先随派工 prompt 交给 `practice-evaluator`'),
 ('必问项之三：前置基础按科目问具体项', '**按科目问具体项**'),
 ('前置基础落 Constraints 与共享记忆', '「科目使命」的 `## Constraints`，随盘问结果一起交给 `curriculum-designer`'),
 ('事实不问学生，但自评必须问', '**前置基础是学生自评、不是事实**'),
 ('事实不问学生', '事实不问学生'),
 ('结束标准 frontier 空', 'frontier 空'),
 ('每轮以「下一步」收尾', '**下一步**：<谁做什么> —— <怎么触发>'),
 ('不重复问同一个问题', '不重复问同一个问题'),
 ('学生说今天到这别再推', '今天到这'),
 ('节点状态置学习中', '学习中'),
 ('派工的值见文件归属表', '按 `<root>/docs/文件归属.md` 角色表「派工时给的值」一列给**值**'),
 ('出题串行交接：内容文件写盘后才派出题人', '锚点是讲解的产物，内容文件没写盘就先别派出题人'),
 ('写盘时题面与答案一个字都不改', '题面与答案一个字都不改'),
 ('empty_reason 由总控用脚本打进内容文件', 'scripts/apply_empty_reasons.py <subject_path> <节点id> <tsv>'),
 ('你写盘 lab 并自查交付态（载体无关）', '参考解全绿、任务/留白态非零退出'),
 ('总控跑渲染器出页面', 'python3 <root>/scripts/render_lesson.py <subject_path> <节点id>'),
 ('渲染失败按 <文件>:<行> 打回对应角色', '按 `<文件>:<行>` 打回'),
 ('实验课说明页：cp 到位后走渲染器（内容格式）', '`cp` 说明页到 `lessons/<序号>-<节点id>.md`'),
 ('实验课与概念课同一条路（渲染 → 检查）', '跟概念课同一条路'),
 ('实验课输入带被验收节点', '被验收节点'),
 ('过检查命令', 'check_lesson.py'),
 ('FAIL 按归属打回', '`FAIL` 按归属打回'),
 ('通过后 present + 绝对路径 + xdg-open', 'xdg-open'),
 ('评估点定义 ①实验课', 'kind: 实验` 的课'),
 ('评估点定义 ②每 3-5 个普通节点', '3-5 个普通节点'),
 ('评估点定义 ③学生要求', '考考我'),
 ('非评估点给保守进度', '初步理解'),
 ('通过则置能独立应用', '能独立应用'),
 ('实验课置位规则', 'record-keeping` 置位'),
 ('部分通过记误解双落点', '双落点'),
 ('两次无效降级 L1', '降级 L1'),
 ('局部提问你亲自答', 'local-qa'),
 ('四份元数据表', '| 「术语表」 |'),
 ('使命变更五步', '使命变更'),
 ('会话结束写摘要', 'session-summary'),
 ('角色规格设了 disable-model-invocation，按绝对路径交给它 read', 'disable-model-invocation'),
 ('角色不写 root', '角色不写 `<root>`'),
 ('派发说清临时产物写科目的 .stage/', '临时脚本与中间产物写 `<subject_path>/.stage/`'),
 ('验收不过退回原 agent', 'send_message'),
 ('只派五类活（收集资料 + 采图 + 大纲/课件/出题）', '只派五类活'),
 ('派工必带三条边界一句', '3. **三条边界**一句：不派子 agent／不用 `ask_user_question`／不建环境（定义见下条）'),
 ('派角色只用全新上下文的 subagent', '不要用 `subagent_fork`'),
 ('fork 注入总控已完成的回合，角色误当总控', '角色会误以为自己是总控'),
 ('预设关掉 fork、subagent 封顶 1 层', '`maxDepth: 1` 兜住'),
 ('角色只干活不调度（不派 agent／不调 ask_user_question／不建环境）', '**不调用 `ask_user_question`**'),
 ('同一科目同时只有一个写入者', '同一科目同时只有一个写入者'),
 ('暂存目录按角色分开，不许共用固定路径', '这类共用固定路径'),
 ('交付报能复算的规模，写盘后核对', '**必须报**能复算的规模'),
 ('搬运用 shell，不转抄（产物只经作者的手）', '**搬运用 shell，不转抄**'),
 ('位↔题交给检查，不手工核', '**「位 ↔ 题」交给检查，你别手工核**'),
 ('评估作答原文（不许摘要）', '**作答原文（逐字转发，不许摘要'),
 ('上游产出给路径，不转述', '**上游产出给路径，不转述**'),
 ('角色规格按路径交，read 读得到', '明说"先读它、照它执行"'),
 ('别把产物读进上下文再写出来', '**别读进上下文再写出来**'),
 ('等子 agent 不许 sleep 轮询（通知会唤醒主线程）', '它那一轮结束会**自动给你送回通知**并把你唤醒'),
 ('要原地等就用有等待语义的调用', '`job_output(wait: true)`'),
 ('草稿路径模板带 slug 变体', "'<subject_path>/.stage/<角色>-<节点id或slug>/'"),
 ('开场默认先写暂存区，不探测工作区', '**默认先写暂存工作区，别去写配置里的 `workspace`、也别做任何探测写入**'),
 ('唯一不暂存的情况：会话目录就是工作区', '**只有一种情况不暂存**'),
 ('暂存根落会话目录，不用 /tmp', '`<WS>` = `<SESSION_DIR>/.studymate-stage/<slug>`'),
 ('明令禁止拿 /tmp 当中转', '别拿 `/tmp` 当中转站'),
 ('暂存模式的两条硬约束', '**暂存模式的两条硬约束**'),
 ('跑引擎脚本加 -B（别往只读引擎目录写 __pycache__）', '跑引擎的 Python 脚本一律加 `-B`'),
 ('暂存模式开场告知学生', '课程先建在会话目录下的临时工作区'),
 ('跨工具接力只在暂存根之下成立', '**每次落盘的下一步要能看见它**'),
 ('暂存模式收尾：问落点并搬走', '问一次成品放哪'),
 ('落点候选：学习工作区／桌面／文档／家目录', '**学习工作区**（配置里的 `workspace`，平时都在那，放进去就能续学）／**桌面**／**文档文件夹**／**用户根目录**'),
 ('有默认落点就排第一并标 Recommended', '**有默认落点就把它放第一项并标"(Recommended)"**'),
 ('落点写进会话侧偏好文件（零提权）', '`<SESSION_DIR>/.studymate-stage/prefs.md`'),
 ('偏好只改一行、不整文件覆盖', '就**只改这一行**，别整文件覆盖'),
 ('开场恢复落点偏好、存在就不重复问', '记成**默认落点**，收尾时它排在第一项、**不再重复问**'),
 ('改全局定位要先问学生', '要改就先问学生一句"以后默认就用这个位置吗"'),
 ('默认不动全局 workspace 键', '`workspace` 是**机器全局**的、在会话写边界外，**不要默默去改**'),
 ('会话结束也要确认落点已持久化', '**落点持久化**：这次收尾搬过东西的话'),
 ('落到工作区时刷新根主页', "gen_home.py '<WS>'"),
 ('cp 搬入后清掉 .stage/', 'rm -rf` 掉 `.stage/'),
 ('新科目并行：RESOURCES.md 落位后同时派两支', '**建池与拟大纲并行**——「资源清单」落位后，同时派下面两个（别串着等）'),
 ('并行两支各自的输入与汇合点一条链', '├─ 「采图」爬「图片库」\n        └─ 「课设」设计大纲\n     两边都回来 → 你写「课程大纲」、核对「图片库索引」→ 开始第一课'),
 ('图片库为空不阻塞', '图片库为空不阻塞'),
 ('并行段终点仍要问开始吗', '→ 开始第一课（仍按「对话节奏」问"开始吗"）'),
 ('后续节点按需补图', 'image-scout`（补图）'),
 ('资源清单收集归资料收集，总控只做小增量', '收集归「资料收集」'),
 ('判断标准：外部检索/大量生成/独立验证', '独立验证'),
 ('跳过节点记原因', '跳过原因'),
 ('单会话 1-2 个节点', '1-2 个节点'),
 ('跨科目只在 MEMORY 共享', '跨科目'),
 ('刷新主页命令', 'gen_home.py'),
 ('大纲插/删节点后跑 renumber 脚本重排重渲', 'python3 <root>/scripts/renumber_lessons.py <subject_path> [--dry-run] [--render]'),
 ('renumber 的时机：报告受影响节点之后、刷新主页之前', '报告受影响节点之后、**刷新主页之前**'),
 ('探索入口先于首次记忆写入', '可选方向入口（首次写记忆之前）'),
 ('明确科目与恢复学习直达原流程', '已有明确科目直接走原流程，恢复已有科目不拦截'),
 ('总控按需加载探索协议', '加载 [`learning-discovery`](../learning-discovery/SKILL.md)'),
 ('探索结束标准不替代新科目盘问', '探索预算不替代本节的结束标准'),
 ('科目前探索不套用科目摘要', '不把探索套入下面的科目摘要与主页刷新规则'),
],
'learning-discovery': [
 ('由总控执行，不增角色', '本协议由你亲自执行，不派新的角色'),
 ('仅显式需要选方向时触发', '仅“不知道学什么”“帮我选方向”或主动选择探索才进入'),
 ('宽泛科目不自动触发', '宽泛的科目名本身不等于同意探索'),
 ('探索只保留会话状态', '只维护会话状态，不写盘'),
 ('复用已有信息与允许读取的记忆', '只读允许读取的 `MEMORY.md` 相关分节，复用本轮表达'),
 ('保留来源与确认状态，不认证自述', '自述不是实测掌握证据'),
 ('确认建课之前不建科目、不写记忆或进度', '用户确认建课之前不创建科目，不写 `MEMORY.md`，不修改掌握度与学习进度，不建立持久化探索档案'),
 ('科目前不生成摘要与主页', '不为探索生成会话摘要、学习记录或刷新主页'),
 ('建课前不做知识小测', '建课前不做有标准答案的知识小测，不生成题库、自动评分或能力认证'),
 ('评估 owner 与输入仍真实', '题目的唯一 owner 仍为 `practice-evaluator`；禁止为调用它伪造 `subject_path`、节点或 assessment'),
 ('一次一问，独立问题不能合并计数', '一次只问一个实质问题'),
 ('禁止以一张卡片包装多个问题', '禁止把多个独立问题塞进一条消息或一张卡片'),
 ('退出只收尾，不报告或继续提问', '只简短收尾，不报告、不提问、不加推动继续的“下一步”'),
 ('跳过探索回选择而不自动建课', '否则回到科目选择，不替学生选方向或自动建课'),
 ('跳过本题仍计数', '当前项记为未知，已问的问题仍计数，再继续必要问题'),
 ('不确定不猜测、不反复逼答', '记为未知，不猜测答案，不换说法反复逼答'),
 ('发问前递增，补问设备问题也计数', '每发一问先令 `asked_count += 1`；补问、追问、冲突确认、设备问题均计数'),
 ('复用不计数，改答案不重置预算', '不能通过换话题、修改答案或重新命名阶段重置预算'),
 ('五问或关键项已覆盖即给阶段建议', '五个关键项已覆盖（包括明确未知），或 `asked_count == 5` 时，先给简短阶段建议'),
 ('信息已够不机械补五问', '足够时可以少于五问，不机械补问五遍'),
 ('五问后仅必要区分才补问', '五问后仅在有必要区分候选时补问'),
 ('八问或要求建议就停止追问', '`asked_count >= 8` 或用户要求先给建议时，哪怕信息不足也必须停问'),
 ('未知时间、设备、基础不编造', '不得假定未知的时间、设备或基础'),
 ('选择菜单不暗藏新信息问题', '建议后的选择菜单不夹带新的信息问题'),
 ('兴趣与自评选项不作推荐', '兴趣、基础、自评、时间和偏好选项不标“推荐”或 `(Recommended)`'),
 ('工具强制推荐则普通对话提问', '若提问工具强制推荐标记，改用普通对话逐题问'),
 ('讲法偏好可变而非人格标签', '偏好只是本次可调整的讲法选择，不是固定人格或能力标签'),
 ('追加探索由用户主动要求并明示', '只有用户主动要求更深入时才加深，明确告知“这是追加探索”'),
 ('禁止自动追加绕过预算', '不得由总控自动开启追加轮来绕过上限'),
 ('入口说明在按需参考文件', '按需读 [七个学习入口](references/directions.md)'),
 ('先修不足不淘汰兴趣方向', '基础不足通常转成先修建议，不直接淘汰兴趣方向'),
 ('二三候选，单一有据时解释', '二至三个简短候选；证据确实只支持一个时说明原因'),
 ('候选附成果与回答证据', '理由对应学生哪条回答；已知基础及证据来源、未知项、必要先修'),
 ('候选有真实取舍而非换名凑数', '候选应在具体成果或主要取舍上有区别'),
 ('候选默认短版但保留决定性约束', '保留能改变选择的先修、设备限制与证据来源'),
 ('共同信息集中说，选择菜单只出现一次', '不逐项重复菜单'),
 ('阶段路径不替代 DAG', '两三阶段概览及主要取舍，不生成正式课程 DAG'),
 ('可比较、修改或暂不决定', '也可比较候选、修改答案、暂不决定'),
 ('不承诺就业或伪精确匹配', '不要承诺职业适配、就业结果、固定精通期限或伪精确的匹配百分比'),
 ('选方向后明示进入开课准备', '方向已选定，接下来进入开课准备，只补齐建课需要的信息'),
 ('交接不回首次三问或写记忆', '不重新走首次使用三问或首次记忆写入'),
 ('交接展示已知与待确认，不要求重填', '首次交接只展示一次简短的“已知／待确认”'),
 ('补问前复用摘要，只澄清具体缺口', '每次补问前先对照会话摘要'),
 ('修改答案只更新受影响决策', '学生修改答案时只更新受影响的决策，不重开整轮访谈'),
 ('单卡不得夹带第二个信息项', '提问工具只放一张卡片，正文不另加问题'),
 ('退出不附续聊口令', '不附“随时回来”“下次说某个口令”等续聊提示'),
 ('已知字段跨轮保留', '本轮未提及的字段保留旧值与来源，不重置为未知'),
 ('五维信息不当作实际问数', '五项信息不等于五次发问'),
 ('工具选型不凑方向数量', '不要把同一成果的 Python／PowerShell 等工具选型硬凑成不同方向'),
 ('建议菜单后不继续问需求', '建议回复写到选择菜单即结束'),
 ('开课前深度载体缺项不能省掉', '没选的深度和载体仍在 frontier'),
 ('首次开始确认前不生成课件', '尚未同意开始时不派 `learning-coach`／`practice-evaluator` 产首课'),
 ('未知不是已回答，盘问仍 frontier 空', '原新科目盘问仍以 **frontier 空** 结束'),
 ('方向成果不等于项目与载体已同意', '方向成果只是候选，不等于项目与载体已获同意'),
 ('开课前回顾确认而非方向即授权', '选择方向不等于同意全部项目与开课操作'),
 ('写入仍由原 owner 执行', '确认后才按总控原落地顺序建科目；所有写入仍由原 owner 执行'),
 ('已确认盘问摘要传正式大纲 owner', '已确认的盘问摘要**随原派工 prompt 传给 `curriculum-designer`'),
 ('保留最终开始确认', '“下一步／开始吗”确认'),
 ('当前节点先同意才开始与更新', '学生同意开始当前节点后才进入首课与进度更新'),
 ('记忆更新仍确认', '需要确认的 `memory_updates` 仍先确认'),
],
'record-keeping': [
 ('工作区目录树', '.learning/'),
 ('MEMORY 四节', '跨科目观察'),
 ('新建科目要建的目录', 'learning-records/'),
 ('assets 只拷三个组件', 'lesson-toc.js'),
 ('lab 不预建', '不预建'),
 ('列出科目的四项', '上次学习日期'),
 ('共享组件更新要同步副本', '共享组件更新后同步'),
 ('图片库与索引归科目自己（不从 templates 同步）', '**「图片库」是科目自己的**：图片与「图片库索引」**不从 `<root>/templates/` 同步**'),
 ('已引用的图不能删', '**已引用的图不能删**'),
 ('图片库随科目打包（区别于 .venv）', '学习产物，随科目整体拷贝或迁移时跟着走'),
 ('图片命名与索引规则指向采图规格', '命名与索引格式见采图规格'),
 ('学习记录命名', 'NNNN-dash-case.md'),
 ('学习记录只记有证据的', '当且仅当出现可观察的证据'),
 ('superseded 不删除', 'superseded by LR-NNNN'),
 ('评估记录命名', 'NNNN-<节点id>.md'),
 ('frontmatter 承载 schema 字段', 'assessment.schema.json'),
 ('逐题依据写进 note，不与大纲逐字对齐', '逐题结论的依据写进该题的 `note`'),
 ('评估记录双落点', '双落点'),
 ('project 只有一句', '只有一句'),
 ('里程碑就是实验节点', '里程碑不在'),
 ('实验通过置位硬规则', '已通过项目验证'),
 ('current 变更先确认', '先跟学生确认'),
 ('主页刷新命令与非零退出', '非零退出看 stderr'),
 ('主页命令显式传工作区根（暂存模式要传临时工作区）', '`python3 -B <root>/scripts/gen_home.py <WS>`'),
 ('刷新时机', '会话结束前'),
 ('增量修改', '增量修改'),
 ('写后对照 schema', 'session-summary.schema.json'),
 ('类型化状态', '类型化的状态'),
 ('会话摘要路径与同日追加', '同日多段追加'),
 ('恢复视图清单', '最近 5 条 misconceptions'),
 ('只读需要的部分', 'offset/limit/grep'),
 ('归属边界', 'practice-evaluator'),
 ('三件文件的关系与 owner', '「课件内容文件」+「练习题库」+「课件页面」'),
 ('档案只留结构化摘要', '原始过程留在会话里'),
 ('评估记录 cp 搬入', '`deliver/assessments/NNNN-<节点id>.md`，**你 `cp` 搬入**'),
 ('科目隔离', '科目之间隔离'),
 ('只写工作区', '绝不写会话目录'),
 ('工作区根默认是会话目录下的暂存区', '路径以**工作区根 `<WS>`** 为前缀'),
 ('暂存模式的额外规则（跨工具接力、.stage 清空、搬走）', '**暂存模式的额外规则**（走暂存工作区时）'),
 ('暂存模式禁止拿 /tmp 当中转（两个命名空间）', '别拿 `/tmp` 当中转'),
 ('角色产出走科目内的 .stage/', '角色产出走科目内的 `<subject_path>/.stage/`'),
 ('交付前 .stage 必须为空', '**交付前 `.stage/` 必须为空**'),
],
'local-qa': [
 ('你亲自答不派 agent', '亲自回答'),
 ('200 字内', '200 字内'),
 ('先答案后为什么', '先给答案，再给一句为什么'),
 ('送回原位', '送回原位'),
 ('超出片段范围交给学习循环', '单独讲'),
 ('落误解库', '「误解库」'),
 ('字段 topic', '`topic`'),
 ('字段 question', '`question`'),
 ('字段 misunderstanding 写无', 'misunderstanding'),
 ('字段 answer_summary', '`answer_summary`'),
 ('字段 follow_up 可选', '`follow_up`'),
 ('字段 importance 三档', 'importance'),
 ('不写 null', '不写 `null`'),
 ('同步进学习进度的 misconceptions', '「学习进度」的 `misconceptions`'),
 ('importance 判据', '前置知识错'),
 ('上下文只留一行', '已处理局部提问'),
 ('只给思路不给完整练习', '完整练习留给学生自己写'),
 ('问第三次转专门讲解', '第三次'),
 ('第三次起别再单点答疑', '别再单点答疑'),
],
'evidence-check': [
 ('核对依据指 objective 与判分要点', '核对依据：「课程大纲」节点的 `objective`'),
 ('证据排序四项', '证据按可信度排序'),
 ('运行结果最先', '运行结果'),
 ('口头确认不足以判过', '单独出现不足以判过'),
 ('L3/L4 交付物逐条核验', 'L3/L4'),
 ('实验课对 prerequisites', 'prerequisites'),
 ('能跑就跑', '能跑就跑'),
 ('运行失败即不成立', '运行失败即证据不成立'),
 ('三档（指向 layered-practice 第五节）', 'layered-practice` 第五节'),
 ('不得因题是我出的判过', '题是我出的'),
],
'layered-practice': [
 ('唯一 owner', 'practice-evaluator'),
 ('字段契约指向 quiz.js', 'quiz.js` 顶部注释'),
 ('题面里的数学写 $…$（LaTeX，页面由 KaTeX 排版）', '**题面里的数学写 `$…$`**'),
 ('题面里的方程组要带大括号', '**方程组要带大括号**'),
 ('四层表 L1 理解', 'L1 理解'),
 ('L2 改造', 'L2 改造'),
 ('L3 排错', 'L3 排错'),
 ('L4 应用', 'L4 应用'),
 ('10-20 行代码', '10-20 行'),
 ('1-2 个 bug', '1-2 个 bug'),
 ('概念止于 L3', '止于 L3'),
 ('L 标不进页面', '不进页面'),
 ('不用 L1-L4 标步骤', '不要用 L1-L4 标练习块'),
 ('四种题型表', '四种题型'),
 ('开放题点击展开', '点击展开'),
 ('选题型原则', '选题型'),
 ('避免教条主义', '教条主义'),
 ('选项等长且检查只 WARN', '选项尽量等长'),
 ('开放题必须给答案与标准', '缺一不可'),
 ('每题备常见错误答案', '常见错误答案'),
 ('实操题可核验', '可核验的'),
 ('≤10 分钟', '≤10 分钟'),
 ('评估题只问理解与权衡', '只问理解与权衡'),
 ('不问机械回忆', '不问机械回忆'),
 ('最多 2 题', '最多 2 题'),
 ('每题最多 3 小问', '每题最多 3 个小问'),
 ('≤5 分钟', '≤5 分钟'),
 ('换行由作者定', '换行位置由作者定'),
 ('枚举小问逐行', '每项各自起一行'),
 ('多行代码写进围栏', '多行代码一律写进'),
 ('散文行长 ≤40 字', '纯散文行（题干、设问、说明）'),
 ('围栏里的代码行 ≤60 字', '围栏里的代码行'),
 ('对齐注释放进围栏', '放进围栏**里照原样对齐'),
 ('选项不换行', '不换行'),
 ('判分要点写进 criteria', '每题的判分要点写进 `criteria`'),
 ('三种课型表', '三种课型'),
 ('实验课任务本身是验收', '任务本身就是验收'),
 ('置位规则指向 record-keeping', 'record-keeping'),
 ('三档结论', '部分通过（列出缺哪条）'),
 ('每条结论指向证据', '指向一条具体证据'),
 ('说懂了不是证据', '说懂了不是证据'),
 ('lab 载体由总控定', 'lab/README.md'),
 ('lab 五条原则：分步引导', '分步引导'),
 ('亲手做/留白', '亲手做'),
 ('可验证', '可验证'),
 ('即时反馈', '即时反馈'),
 ('卡壳先引导', '卡壳先引导'),
 ('教程带结果', '教程部分要带结果'),
 ('答案与成品分开放', 'solutions/`，做完才对照'),
 ('lab 目录结构', 'lab/NNNN-主题/'),
 ('环境依赖写 README', 'lab/README.md'),
 ('题目位置写成 ::: quiz 指令 + 锚点', '::: quiz <层级> 锚点：<锚点文本>'),
 ('无题锚点必须写 empty_reason', 'empty_reason: <理由>'),
 ('练习段落不写也不接受 empty_reason', '不写、也不接受 `empty_reason`'),
 ('::: practice 层级徽标的合法词汇（练习／上手做）', '写 `练习`／`上手做` 这类中文阶段名'),
 ('实验说明页也交内容格式正文（语法见格式文档）', '说明页也是内容格式正文'),
],
'practice-evaluator': [
 ('输入节标题统一', '## 输入（总控在 prompt 里给）'),
 ('你是唯一 owner', '全系统的题都由你出'),
 ('你不写科目目录（产出落 deliver/）', '你不写**科目目录**'),
 ('你一个字 HTML 都不写（说明页也交内容格式）', '你一个字 HTML 都不写'),
 ('先加载两份协议', '先加载'),
 ('说明页与正文的语法指向格式文档', '实验说明页与课件正文的语法见 `<root>/docs/课件内容格式.md`'),
 ('字段契约', 'quiz.js'),
 ('输入含 kind', '`kind`'),
 ('输入含 problem 与 practice（侧重自己读）', '`objective`／`problem`／`practice`／`kind`／`prerequisites`'),
 ('层级与题量跟着侧重走', '**层级与题量再跟着这课的侧重走**'),
 ('以练为主：往 L3／L4 压、少而深', '`以练为主` 的节点把题往 L3／L4 压、题量少而深'),
 ('以练为主也不越 kind 的上限', '**上限仍由 `kind` 定**'),
 ('以讲为主：理解型题为主', '`以讲为主` 的节点以理解型题为主'),
 ('输入含实操载体', '实操载体'),
 ('载体没有就问总控', '别自己选'),
 ('输入含目标层级默认 L2', '默认从 L2 起试'),
 ('实验课另给被验收节点', 'prerequisites'),
 ('时机一：概念题按规范', 'layered-practice` 第二、三节'),
 ('实操要整套 lab', '整套 lab 内容'),
 ('实验不产课件题', '不产课件题'),
 ('实验说明页四项', '自查清单'),
 ('说明页要指向实操任务', '指向实操任务'),
 ('评估记录由出题人写盘', '`deliver/assessments/NNNN-<节点id>.md`'),
 ('lab README 整份写盘', '**`lab/README.md` 整份**'),
 ('节点字段按 id 自己读（不贴全文）', '**别让总控贴节点全文**'),
 ('题库落 deliver/（正文不贴全文）', '`deliver/<序号>-<节点id>.quiz.json`'),
 ('实验课行：说明页是内容格式正文', '实验说明页正文（**内容格式，不是 HTML**）'),
 ('最可能卡在哪', '最可能卡在哪'),
 ('交付逐题标层级与判分要点', '逐题标注'),
 ('时机二只在评估点', '阶段评估点'),
 ('时机二走证据核验', '证据核验'),
 ('返回六项', '建议掌握度'),
 ('三档与证据指向规范', 'layered-practice` 第五节'),
 ('口头确认不足', '口头确认单独出现不足以判过'),
 ('答案不唯一接受替代', '替代方案'),
 ('定位误解根源', '误解根源'),
 ('不替学生写代码', '不替学生写项目代码'),
 ('题目写盘、正文给短表', '锚点 ↔ 层级 ↔ 判分要点 的短表'),
 ('出题逐锚点交题或交一行 empty_reason（此规则归本角色规格）', '按锚点清单逐个锚点出题'),
 ('题目按锚点写成练习题库（键逐字对应，结构见格式文档 §4）', '键与「课件内容文件」的锚点逐字对应，对不上渲染器直接报错'),
 ('不改课程与档案', '不改课程与档案文件'),
],
'resource-scout': [
 ('输入节标题统一', '## 输入（总控在 prompt 里给）'),
 ('只读权威教材与官方文档', '权威教材（公认教材、经典书、同行评审材料）与官方文档'),
 ('清单分两类：延伸阅读 + 易变内容的官方核对来源', '**易变内容的官方核对来源**：每条'),
 ('每条带 title/type/url（缺一项不算一条）', '每条资源都必须带 `title`/`type`/`url` 三项 + 一行用途，缺一项就不算一条'),
 ('收手三条之一：找不到的部分能列成 ## Gaps', '找不到的部分能列成 `## Gaps`'),
 ('不写大纲（大纲归 curriculum-designer）', '不写大纲——大纲是 `curriculum-designer` 的产出'),
 ('清单落 deliver/RESOURCES.md，正文只报摘要', '`deliver/RESOURCES.md`'),
],
'image-scout': [
 ('输入节标题统一', '## 输入（总控在 prompt 里给）'),
 ('命名规则串：格式 + 字符集 + 无空格 + ≤60', '`<主题>-<子主题>-<要点>-<来源缩写>-<NN>.<ext>`，只用 `[0-9A-Za-z\\u4e00-\\u9fa5-]`、无空格、总长 ≤60'),
 ('索引是图片库索引，表头七列逐字固定', '写在「图片库索引」，表头逐字固定：`| 文件 | 主题标签 | 一句话说明 | 来源 URL | 许可 | 尺寸 | 抓取日期 |`'),
 ('索引末尾 ## Gaps', '## Gaps'),
 ('只抓网页：不抓 PDF、不抓动态加载、不漫游搜索', '不抓 PDF、不抓动态加载、不漫游搜索'),
 ('站点边界：点名的 + 总控另给的额外站点', '**站点边界** = 清单点名的站点 + 总控另给的"额外允许站点"'),
 ('站内搜索/分类/画廊页可用，最多两跳', '**站内搜索/分类/画廊/条目页**去找'),
 ('每站预算：8 页 / 6 张', '最多访问 8 个站内页面、最多下载 6 张图'),
 ('同站低频串行（≤1 请求/秒）、尊重 robots', '同站低频串行（≤1 请求/秒）、尊重 robots'),
 ('来源与许可：页面没标注就填「未标注」', '页面没写就填「未标注」'),
 ('许可是记录不是门槛（不为它放弃图/做核查）', '**许可是记录、不是门槛**（个人学习用途）'),
 ('交付：写盘即交付（不写别处）', '写盘即交付'),
 ('交付：正文只报五项（图片库路径/张数/索引路径/Gaps/最值得用的 3~5 张，此规则归本角色规格）', '正文只报五项：「图片库」路径、张数、「图片库索引」路径、`Gaps` 条数、**最值得用的 3~5 张**'),
 ('边界：你只搬现成图（自己产图走另一条路）', '自己产图走另一条路（归「讲解」'),
 ('边界：不写课件、不做课程（归属指向 record-keeping）', '课件三份产物的归属见 `record-keeping` 的「课件三份产物的归属」'),
 ('收图体积上限 500 KB（与课件硬约束同一个数）', '**单张 >500 KB 的也不要**'),
 ('采图按条目点名，URL 自己读', '**URL 你自己从文件里读，别让总控抄一遍**'),
],
'curriculum-designer': [
 ('输入节标题统一', '## 输入（总控在 prompt 里给）'),
 ('输入六项', '盘问结果'),
 ('按依赖建 DAG', 'DAG'),
 ('节点粒度', '40分钟能完成一个学习单元'),
 ('第一个节点是绪论', '第一个节点是绪论'),
 ('kind 三选一', '三选一：`概念`'),
 ('problem 是问题不是定义', '是问题，不是定义'),
 ('problem 要点明价值在讲清概念还是练熟动作', '并点明这课的价值在**讲清某个概念**还是**练熟某个动作**'),
 ('practice 明写侧重三选一（讲／练／并重）', '写 `以讲为主`／`以练为主`／`讲练并重` 三者之一'),
 ('practice 不许写配比、时长、数目', '**不写配比、时长、数目**'),
 ('侧重默认跟着 kind 走（方向不是配额）', '**侧重不许与 `kind` 打架**'),
 ('深度对齐目标层级', '目标层级'),
 ('resources 易变要官方文档', '官方文档'),
 ('资源每条带 title/type/url', 'title`/`type`/`url`'),
 ('title ≤16 字', '≤16 字'),
 ('objective ≤34 字', '≤34 字'),
 ('按需写不为整齐填满', '逐个填满'),
 ('标题句式自查', '同一个句式'),
 ('实验课与普通课同级', '与普通课同级'),
 ('实验节点字段', '实验一'),
 ('prerequisites 至少一个真实 id', '真实存在的 id'),
 ('3-5 个实验', '3-5 个'),
 ('实验递进', '递进'),
 ('最可能卡在哪', '最可能卡在哪'),
 ('项目未选定给多版草案', '草案'),
 ('别全标实操', '别把所有节点都标成实操'),
 ('三条线索', '最终目标线索'),
 ('资源清单两类+Gaps', 'Gaps'),
 ('调整课程的限制', '无环'),
 ('保留已学节点', '保持原样'),
 ('同步实验课', '同步实验课'),
 ('校验命令', 'check_curriculum.py'),
 ('环检测', '环检测'),
 ('& 与 * 必须引号', '必须加引号'),
 ('报告内容', '三条线索怎么落'),
 ('只报告不改', '报告即可'),
 ('边界：待总控核实', '待总控核实'),
 ('不写学习进度与四份元数据', '不写「学习进度」'),
 ('不出题', '不出题'),
 ('不盘问用户', '不盘问用户'),
 ('交付报规模与校验器原文输出', '交付时报出规模'),
 ('大纲落 deliver/curriculum.yaml', '`<subject_path>/.stage/curriculum-designer-<slug>/deliver/curriculum.yaml`'),
 ('不派子 agent、不调用 ask_user_question', '**不盘问用户、不派子 agent**'),
 ('草稿只写总控指定的暂存目录', '草稿只写总控指定的暂存目录'),
 ('位次挪动：报告受影响节点，命令与时机指向 learning-system', '改名与重渲的命令与时机见 `learning-system`'),
],
'learning-coach': [
 ('输入节标题统一', '## 输入（总控在 prompt 里给）'),
 ('题目不归你', '题目不归你'),
 ('lab 内容不抄进课件', '不抄进课件'),
 ('先加载 lesson-design', '先加载 `lesson-design`'),
 ('输入含科目使命', '「科目使命」（回扣目标）'),
 ('输入含 kind 与实验课不走你', '实验` 不走你'),
 ('输入含 problem 与 practice（侧重自己读）', '`objective`／`problem`／`practice` 自己从「课程大纲」读'),
 ('篇幅按节点侧重安排', '**篇幅按节点侧重走**'),
 ('以讲为主：讲解配图为主体、练习只一处轻量跟做', '讲解与配图是主体，代码示例少而短，练习只留一处轻量跟做'),
 ('以练为主：讲解只作引入', '讲解只作引入，动手是主体'),
 ('讲练并重：讲一段立刻练一段', '`讲练并重` → 讲一段立刻练一段'),
 ('侧重不写成比例', '**不要写死比例**'),
 ('产出课件内容文件（不是页面）', '产出「课件内容文件」'),
 ('复用组件术语一致', '术语保持一致'),
 ('术语表为准', '「术语表」为准'),
 ('文件名规则', '<序号>-<节点id>.md'),
 ('与上一课指针逐字一致', '逐字一致'),
 ('练习段落写 ::: practice', '::: practice <层级>'),
 ('概念课轻量练习', '轻量练习'),
 ('返回三项', '要点摘要'),
 ('题目位置写 ::: quiz + 锚点', '::: quiz <层级> 锚点：<锚点文本>'),
 ('留题目位置，不写题', '留题目位置，不写题'),
 ('empty_reason 由出题人定，讲解不写', '`empty_reason` 由出题人定，你不写'),
 ('::: practice 的层级是徽标（写中文阶段名）', '会显示成徽标'),
 ('每个指令块都要有单独一行 ::: 收尾', '每个指令块都要有单独一行 `:::` 收尾'),
 ('交稿前 --check 只修语法类报错', '题库/锚点类报错此时是预期的'),
 ('渲染器章节：只交内容，结构由渲染器产出', '这些由渲染器从内容文件产出，你交内容就行'),
 ('渲染器章节：页面结构归渲染器', '**页面结构**：`head` 与共享层引用'),
 ('渲染器章节：上下节课指针归渲染器', '**上下节课指针**：按「课程大纲」的 `nodes:` 顺序算'),
 ('渲染器章节：图注编号归渲染器', '**图注编号**：按页内出现顺序自动编号'),
 ('渲染器章节：图注来源与许可归渲染器', '**图注的来源与许可**：按「图片库索引」那一行自动补'),
 ('渲染器章节：公式排版归渲染器', '**公式排版**：注入离线 KaTeX'),
 ('渲染器章节：成品样式归容器', '**图注与页面的成品样式**：字体、间距、宽度都由容器定'),
 ('交稿前 render_lesson.py --check 自检', 'render_lesson.py <subject_path> <节点id> --check'),
 ('交稿只报想要的题型（锚点在文件里）', '每处锚点**想要的题型**'),
 ('速查页 reference/', 'reference/'),
 ('边界：只做当前节点', '只做当前节点'),
 ('待学清单', '待学清单'),
 ('不碰课程与档案', '不碰课程与档案文件'),
 ('配图：写课件前先读图片库索引', '**先读「图片库索引」**'),
 ('配图：挑到就用图片库里的相对路径', '../assets/img/pool/<文件>'),
 ('配图：挑不到就自己产，画法自己挑', '**挑不到就自己产**：**画法自己挑**'),
 ('配图：自产图留三样（图/脚本/索引行）', '**自己产的图要留三样东西**'),
 ('配图：脚本落 assets/img/gen/<同名>.py', '`assets/img/gen/<同名>.py`'),
 ('配图：自产图的索引行三列怎么写', '`来源 URL` 写「本机生成（`assets/img/gen/<名字>.py`）」'),
 ('配图：pool.md 追加一行', '「图片库索引」**追加**一行'),
 ('配图：来自图片库或自己产', '**图来自「图片库」或自己产**'),
 ('配图：真实照片/界面交总控派 image-scout 采集', '真实照片/界面这类只能采集的，交总控派 `image-scout`'),
 ('配图：索引的列与命名格式指向 image-scout', '索引的列与文件命名格式见 `image-scout`'),
 ('生成图可裁可缩，成品 ≤500 KB', '**可以裁、缩、换配色**'),
 ('matplotlib 默认字体不出汉字、需指定 CJK', 'matplotlib **默认字体不出汉字**'),
 ('每张图必须有 alt:', '每张必须有 `alt:`'),
 ('渲染器章节：公式排版归渲染器（TeX 是它的输入）', '**公式排版**：注入离线 KaTeX，你只管写 TeX：行内 `$…$`、块级 `$$…$$`'),
 ('caption 只写一句话', '`caption:` 只写一句话'),
 ('课件别依赖外链，图要能离线打开', '别依赖外链**（页面里的图要能离线打开）'),
 ('课件页面内单张 ≤500 KB', '≤500 KB'),
 ('着眼点照顾到、版式自定', '着眼点'),
 ('输入含 lab 载体（实操才有）', '`kind: 实操` 才有小 lab，给载体与位置'),
 ('自检别消掉预期的题库报错', '尤其别补 `empty_reason`'),
 ('lab 内容不抄进课件、课件只指向它', '课件只指向它'),
 ('不碰课程与档案（归总控）', '归总控'),
],
'lesson-design': [
 ('管内核不管版式', '不管版式'),
 ('共享记忆偏好优先', '「共享记忆」的个人偏好优先'),
 ('题目 lab 不归它管', '题目、lab、实验说明页也不归本文件管'),
 ('着眼点之参考教材', '收集到的认可度高的教材'),
 ('权威教材深度', '不做速成课'),
 ('实操/概念两种课型的练习', '引导式实操'),
 ('练习块标题写中文阶段名（L1-L4 不进页面）', '不要用 L1-L4 标步骤'),
 ('三条贯穿线索', '贯穿线索'),
 ('h2 ≤14 字', '≤14 字'),
 ('起点：先读组件库、语法以格式文档为准', '语法以 `<root>/docs/课件内容格式.md` 为准'),
 ('顶栏与主题开关归渲染器', '顶栏、主题开关、脚本引用由渲染器产出'),
 ('命名规则', '<序号>-<节点id>.md'),
 ('配图用 ::: figure 指令', '::: figure <相对路径>'),
 ('内联示意图可用 ::: svg（不是唯一画法）', '内联示意图另有 `::: svg`'),
 ('该配图处不用文字带过（配几张与画法不限）', '"前后对比、参数影响、结构/流程关系、数据分布"这类内容，用图比用文字省力'),
 ('指针由渲染器算', '渲染器按「课程大纲」的 `nodes:` 顺序算'),
 ('第一课/最后一课由渲染器算', '第一课没有上一个、最后一课没有下一个'),
 ('内容文件里不写 HTML 标签', '出现块级 HTML 标签会被渲染器拦下'),
 ('题目位置只留位，题归出题角色', '只留位、不写题'),
 ('渲染产物自包含', '渲染出的页面自包含'),
 ('反复回看', '反复回看'),
 ('行文禁忌三条', '肯定-否定'),
 ('不生硬修辞', '生硬的修辞'),
 ('不代入用户视角', '代入用户视角'),
 ('着眼点是弱约束（可按实际调整）', '弱约束：按实际情况调整'),
 ('配套项目从早期就逐课埋线', '从早期就逐课埋线'),
 ('列表项不写长段落', '列表项不写长段落'),
 ('h2 句式服从内容', '句式服从内容'),
 ('提示块不拿它凑版面', '不拿它凑版面'),
 ('缺的来源报总控', '缺的来源报给总控'),
 ('归属只认文件名（eyebrow 不影响）', '写不写节点 id 都不影响归属'),
 ('行文禁忌：别假定学生往错的方向想', '不要先假定学生会往错的方向想'),
 ('行文禁忌：代入视角的示例', '编译落到你手上'),
 ('行文禁忌：别为帮理解反而加重负担', '反而加重用户负担'),
],
}

bad = 0
total = 0
for skill, rules in RULES.items():
    text = open(os.path.join(SK, skill, 'SKILL.md'), encoding='utf-8').read()
    missing = [(desc, frag) for desc, frag in rules if frag not in text]
    total += len(rules)
    if missing:
        bad += len(missing)
        print(f'FAIL {skill}: {len(missing)}/{len(rules)} 条规则在新版里找不到')
        for desc, frag in missing:
            print(f'       · {desc}  （找的是 {frag!r}）')
    else:
        print(f'PASS {skill}: {len(rules)} 条规则全部在位')

# 这是静态协议检查，不执行模型，也不能证明真实对话会遵守问数与写入边界。
def contract(label, condition):
    global bad, total
    total += 1
    bad += not condition
    print(f"{'PASS' if condition else 'FAIL'}  {label}")


discovery_dir = Path(SK) / 'learning-discovery'
discovery = (discovery_dir / 'SKILL.md').read_text(encoding='utf-8')
reference_links = re.findall(r'\]\((references/[^)]+)\)', discovery)
references = [discovery_dir / link for link in reference_links]
contract('探索协议仅引用一份实际存在的本地参考文件',
         len(references) == 1 and references[0].is_file())
reference_text = references[0].read_text(encoding='utf-8') if len(references) == 1 and references[0].is_file() else ''
entrances = re.findall(r'^\| ([^|]+) \|', reference_text, re.M)[1:]
contract('参考表保持七个可交叉入口', entrances == [
    '应用开发与自动化', '数据分析与数据工程', '机器学习与 AI 原理',
    '系统、可靠性与性能', '图形与游戏开发', '嵌入式、机器人与控制', '数学探索与基础',
])
contract('设备路径先确认条件，设备问题也计入预算',
         '先询明设备条件，再提出具体路径' in reference_text
         and '设备问题另轮单问且计数' in reference_text)
system = (Path(SK) / 'learning-system' / 'SKILL.md').read_text(encoding='utf-8')
interview = system.split('## 新科目盘问（你亲自执行）', 1)[-1].split('## 对话节奏', 1)[0]
contract('原新科目盘问保留七步及 frontier 空结束规则',
         re.findall(r'^([1-7])\. ', interview, re.M) == list('1234567')
         and '6. **结束标准**：frontier 空' in interview)
contract('总控保持指针接入，不复制七入口参考表',
         '| 应用开发与自动化 |' not in system and '| 数学探索与基础 |' not in system)

# 只检查合成场景文件可用，不发送模型请求，也不把其 checks 当作已通过。
cases = json.loads((Path(__file__).parent / 'fixtures' / 'learning_discovery_cases.json')
                   .read_text(encoding='utf-8'))['cases']
case_ids = [case['id'] for case in cases]
contract('十二个对话场景标识不重复', len(case_ids) == len(set(case_ids)) == 12)
contract('对话场景有用户输入、后续回复、停止条件和独立判据', all(
    isinstance(case.get('initial_user'), str) and case['initial_user'].strip()
    and isinstance(case.get('adaptive_answers'), dict)
    and isinstance(case.get('turns'), list)
    and all(turn.get('when') and turn.get('user') for turn in case['turns'])
    and case.get('stop_when') and case.get('checks')
    and isinstance(case.get('max_user_turns'), int) and case['max_user_turns'] > 0
    for case in cases))

print(f'\n合计 {total - bad}/{total} 条规则在位')
sys.exit(1 if bad else 0)
